# Deployment

Deployment-related files will be grouped by target:

- `aws/` for VM setup and execution notes
- `docker/` for container-based local orchestration
- `nginx/` for the direct architecture entry point
- `systemd/` for service management on Linux VMs
